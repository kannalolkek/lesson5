users = {'Оля': 'дитина', 'Іван': 'підліток', 'Марія': 'дорослий', 'Петро': 'літня людина' }

name = input('введіть імя користувача:')

if name in users:
    print(f'{name} належить до вікової групи: {users[name]}')
else:
    print('користувача з таким імям не знайдено.')
